# Redis configuration for development
# This file is for local development only - do not use in production

# Enable append-only file for persistence
appendonly yes

# Require authentication (password will be set via environment)
# Note: This is a development-only configuration
# Production should use proper ACL files and secure authentication