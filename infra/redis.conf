# Redis configuration

# Bind to all interfaces within Docker network
# Security is enforced via requirepass (set via command line)
bind 0.0.0.0

# Protected mode disabled since we use password authentication
# and Redis is only accessible within Docker network
protected-mode no

# Persistence
appendonly yes
dir /data
